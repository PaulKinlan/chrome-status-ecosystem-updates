version: 2
updates:
  # Keeps the SHA-pinned actions in .github/workflows current. Without this,
  # pinning to a SHA means silently missing security fixes.
  - package-ecosystem: 'github-actions'
    directory: '/'
    schedule:
      interval: 'weekly'
    commit-message:
      prefix: 'ci'
